/**
 * Program constants — single source of truth.
 *
 * PROGRAM_START: Monday 2026-02-16
 * All week numbers and weekday assignments derive from this date.
 * UTC arithmetic is used everywhere to avoid DST issues.
 */
export const PS_UTC = Date.UTC(2026, 1, 16);
